import {} from "react";

import st from "./styles.module.scss";
import cn from "classnames";

type SortProducts = {};

export const SortProducts = (props: SortProducts) => {
  return <>SortProducts</>;
};
