import {} from "react";

import st from "../../../styles.module.scss";
import cn from "classnames";

type BlogChunk = {};

export const BlogChunk = (props: BlogChunk) => {
  return <>BlogChunk</>;
};
